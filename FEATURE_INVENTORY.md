# 当前功能清单

源码基线：**v2.18**。本文描述已实现的行为；商店可用版本以商店页面为准。历史方案不作为当前功能承诺。

## 四种模式，四个阅读任务

| 模式 | 适合的任务 | 展开后的行为 |
| --- | --- | --- |
| 标准面板 | 查看完整结构，反复查阅文档 | 层级目录、当前章节、Top / Close；支持悬停、点击和长按交互设置 |
| 邻节预览 | 确认附近的跳转位置 | 仅显示目标标题与前后各一项；复用三个预览节点 |
| 检索目录 | 记得关键词，快速找章节 | 完整可滚动目录；多个关键词匹配标题；方向键选择、Enter 跳转 |
| 页边大纲 | 长时间对照章节阅读 | 利用正文外侧留白；可固定展开；空间不足时退回视口边缘 |

设置入口分为「标准面板」和「边缘导航」，后三种模式属于边缘导航。旧 `wheel` 配置自动迁移至 `spotlight`（邻节预览）；`gpt` 与 `sspai` 是兼容存储标识，不表示联网 AI 或第三方服务。

## 共同能力

- 从正文区域提取标题与层级，过滤隐藏、导航及侧栏内容；识别结果取决于网页结构。
- 高亮当前章节，点击标题跳转，Top 回到顶部。
- 响应 DOM 与常见 SPA 导航变化，重新识别内容。
- 根据页面及导航附近背景选择明暗表面；每种模式使用中性色与一种功能强调色。
- 支持左右位置、禁用域名、已有目录/回顶控件避让和强制显示。
- 折叠目录不占用多余 Tab 停靠点；方向键、Home / End 选择标题，Enter 激活，Esc 收起。检索框内方向键可进入匹配结果。

## 默认设置与保存

| 设置 | 默认值 |
| --- | --- |
| 导航类型 | 标准面板 |
| 标准面板交互 | 悬停预览 |
| 边缘导航样式 | 邻节预览 |
| 最少标题 | 3 |
| 滚动距离 | 1 屏 |
| 位置 | 右侧 |
| 自动避让已有控件 | 开启 |
| 强制显示 | 关闭 |

点击「保存更改」后，以页面显示的保存状态为准；保存失败会提示。保存成功后刷新已打开的文章页面应用配置。同步设置由 Chrome 扩展存储管理。

## 检索范围与隐私

检索目录只筛选**当前文章的标题**，不搜索正文、不调用 AI、不发送网络查询。支持大小写、全角字符归一化和多个关键词。没有结果时提供空状态；关闭预览会清除查询。查询不写入设置存储。

页面结构、阅读位置和背景颜色在页面内处理，不上传正文。`storage` 保存偏好；`<all_urls>` 用于在不同网站运行内容脚本。浏览器内部页面和受限页面不能像普通网页一样注入扩展。

## 低干扰与性能边界

- 边缘预览有短暂停留判定，避免路过即展开；移开、Esc 或隐藏导航会清理预览。
- 没有循环装饰动画和背景模糊；回顶入口静态显示，不持续跟踪全文指针距离。
- 邻节预览的更新节点数量固定，不为每个标题创建滚动轨道或逐项波动动画。
- 标题检索复用预处理后的标题文本；当前章节优先由 IntersectionObserver 追踪。
- 尊重系统「减少动态效果」，覆盖目录动画、章节跳转与回顶。
- 这些是实现约束，不等同于所有网站上的零性能开销；长文、动态页面和复杂布局仍需实测。

## 实现与交付索引

- 内容识别、目录与交互：`catalog.js`
- 主题与位置适配：`theme.js`、`toc.css`、`themes.css`
- 偏好与旧配置兼容：`options.js`、`options.html`
- 本地多标题验证页：`test-pages/rail-hover-performance.html`
- 品牌与导出说明：`icons/README.md`
- 商店素材与上传顺序：`output/chrome-store/v2.18/README.md`
- 打包、GitHub Release 与手动商店上传：`CHROME_STORE_RELEASE.md`
